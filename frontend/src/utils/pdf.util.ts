// TODO: implement — stub for build compatibility

export const downloadCompareTableAsPDF = (
  elementId: string,
  fileName?: string,
  _data?: unknown,
): void => {
  // Stub: real implementation would use a PDF library (e.g., jspdf, html2pdf)
  console.info('downloadCompareTableAsPDF called', { elementId, fileName });
};
